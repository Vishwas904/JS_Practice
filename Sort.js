//Sorting the array in Ascending Order using the sort method 
//Ascendong Order
let compare = (a, b) => {
    return a-b
  }
  console.log(line.sort(compare))

  //Descending Order
//   let compare = (a, b) => {
//     return b-a
//   }
//   console.log(line.sort(compare))

